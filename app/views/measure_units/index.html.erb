<div class="container">
  <div class="row">
    <div class="col">
      <div class="card">
        <div class="card-header d-flex justify-content-between">
          <div>
            <button type="button" class="btn btn-light">Unidades de Medidas</button>
          </div>
          <div>
          </div>
          <%= link_to 'Nueva Unidad de Medida', new_measure_unit_path, :class=> "btn btn-primary" %>
        </div>
        <div class="card-body">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Nombre</th>
                <th colspan="3"></th>
              </tr>
            </thead>
            <tbody>
              <% @measure_units.each do |measure_unit| %>
                <tr>
                  <td><%= measure_unit.name %></td>
                  <td><%= link_to ('<i class="fa fa-search"></i>').html_safe, measure_unit %></td>
                  <td><%= link_to ('<i class="fa fa-pencil"></i>').html_safe, edit_measure_unit_path(measure_unit) %></td>
                  <td><%= link_to ('<i class="fa fa-trash"></i>').html_safe, measure_unit, method: :delete, data: { confirm: '¿Esta seguro de eliminar el registro?' } %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<br>
